.set(1,1,1); u.hiBody.visible=u.hiSurface.visible=u.rainbow.visible=false; wet.intensity=0;u.waterMat.color.copy(BASE_WATER); u.waterMat.emissive.set(0x071a2b); u.waterMat.opacity=.67; u.waterMat.shininess=18; u.surfaceMat.color.copy(BASE_SURFACE); u.surfaceMat.emissive.set(0x091a28); u.surfaceMat.opacity=.76; u.surfaceMat.shininess=20;u.leaf.material.color.set(0x688f43); u.leaf.material.emissive.set(0x10180a); u.leafGroup.scale.set(1,1,1); u.leafGroup.position.set(.16,.485,-.03); u.leafGroup.rotation.set(.055,.54,-.045);clear(u.crystals); clear(u.bubbles); clear(u.clones); u.overflow.children.forEach(x=>{x.visible=false; if(x.material) x.material.opacity=0}); u.echoes.children.forEach(x=>{x.visible=false;if(x.material)x.material.opacity=0});if(stateType==='conjure')spawnCrystals(stateLevel); if(stateType==='specialize'){const c=specialRecipe.secondaries.find(x=>x.type==='conjure'); if(c)spawnCrystals(c.level,seed+123); if(specialRecipe.anomalies.includes('boil'))spawnBubbles(10+stateLevel*6); if(specialRecipe.anomalies.includes('clone'))spawnClones(stateLevel===3?2:1)}runStart=performance.now(); firstDotAt=runStart+900; updateDebug(); }function effect(type,level,p){const q=ease(p), o={rise:0,jit:0,hi:0,hue:null,hm:0,rb:0,fl:0,cry:0,ov:0}; if(type==='enhance'){o.rise=[0,.20,.50,.92][level]*q; if(level===3)o.ov=clamp((p-.56)/.44)} if(type==='manipulate')o.jit=[0,.018,.055,.13][level]*q; if(type==='transmute')o.hi=[0,.95,1.25,1.65][level]*q; if(type==='emit'){o.hue=emitHue; if(level===1)o.hm=.46*q; else if(level===2)o.hm=.96*q; else if(emitMode==='flash')o.fl=q; else o.rb=q} if(type==='conjure')o.cry=q; return o }function merge(a,b,w=1){for(const k of ['rise','jit','hi','hm','rb','fl','cry','ov']) a[k]=Math.max(a[k]||0,(b[k]||0)*w); if(b.hue!==null) a.hue=b.hue; return a}function updateDots(now){if(now<firstDotAt){revealEl.textContent='您的念能力是'; return 0} const t=now-firstDotAt, dots=Math.min(5,1+Math.floor(t/430)); revealEl.textContent='您的念能力是'+'.'.repeat(dots); return clamp(t/2150)}function updateDebug(){const em=stateType==='emit'?` / COLOR HUE ${Math.round(emitHue*360)}° / Lv3 ${emitMode==='bands'?'SEED-BANDS':'SEED-FLASH'}`:''; const sp=stateType==='specialize'?` / SPECIAL ${specialRecipe.secondaries.map(x=>x.type+':'+x.level).join(',')} / ${specialRecipe.anomalies.map(x=>ANOMALY_ZH[x]||x).join(',')}`:''; debugEl.textContent=`TYPE: ${TYPES[stateType]} / Lv${stateLevel} / SEED ${seed}${em}${sp}`}function showResult(){resultName.textContent=TYPES[stateType]; resultScore.textContent=`PREVIEW / Lv${stateLevel}`; let more=NOTE[stateType]; if(stateType==='emit')more+=`\n抽色：${Math.round(emitHue*360)}° / Lv3 ${emitMode==='bands'?'色帶':'閃爍'}`; if(stateType==='specialize')more+=`\n異常：${specialRecipe.anomalies.map(x=>ANOMALY_ZH[x]||x).join('、')}`; resultNote.textContent=more; resultEl.classList.add('show')}function resize(){const w=innerWidth,h=innerHeight; camera.aspect=w/h; camera.updateProjectionMatrix(); const scale=Math.max(.55,Math.min(.8,520/Math.max(w,h))); renderer.setSize(Math.round(w*scale),Math.round(h*scale),false); renderer.domElement.style.width=w+'px'; renderer.domElement.style.height=h+'px'} addEventListener('resize',resize); resize();let shown=false, last=0;function anim(now){requestAnimationFrame(anim); if(now-last<1000/12) return; last=now; const s=now*.001; root.rotation.y=.3+s*.14; root.rotation.x=Math.sin(s*.34)*.02; const p=updateDots(now); const u=model; let par={rise:0,jit:0,hi:0,hue:null,hm:0,rb:0,fl:0,cry:0,ov:0};if(stateType!=='specialize') merge(par,effect(stateType,stateLevel,p)); else {for(const sec of specialRecipe.secondaries) merge(par,effect(sec.type,sec.level,p), sec.type==='emit'?1:.88); const base=clamp(p*1.05); const an=specialRecipe.anomalies; var wither=an.includes('wither')?ease(base):0, boil=an.includes('boil')?ease(base):0, black=an.includes('blackwater')?ease(base):0, hover=an.includes('hover')?ease(base):0, echo=an.includes('echo')?ease(base):0, pulse=an.includes('pulse')?ease(base):0, glitch=an.includes('glitch')?ease(base):0, reverse=an.includes('reverse')?ease(base):0, timeskip=an.includes('timeskip')?ease(base):0; }const riseRaw=par.rise + (stateType==='specialize'&&hover?hover*.16:0); const insideRise=Math.min(riseRaw,.73), overflowExcess=Math.max(0,riseRaw-.73), newH=BASE.height+insideRise; u.water.scale.y=newH/BASE.height; u.water.position.y=BASE.bottom+newH/2; u.surface.position.y=BASE.top+insideRise; u.hiBody.scale.y=newH/BASE.height; u.hiBody.position.y=u.water.position.y; u.hiSurface.position.y=u.surface.position.y;let bx=.16+Math.cos(s*.78)*.035, bz=-.03+Math.sin(s*.72)*.04, by=u.surface.position.y+.026, rx=.055+Math.sin(s*.85)*.017, ry=.54+Math.sin(s*.52)*.032, rz=-.045+Math.cos(s*.67)*.021;if(par.jit>0){const freq=stateLevel===1?18:stateLevel===2?9:6; const k=Math.floor(s*freq), n1=noise(k,seed), n2=noise(k+33,seed), n3=noise(k+71,seed); bx+=n1*par.jit; bz+=n2*par.jit*.75; rz+=n3*par.jit*2.6; ry+=n1*par.jit*2; if(stateLevel===3&&Math.abs(n2)>.72) rx+=n2*.18*ease(p)}u.leafGroup.position.set(bx,by,bz); u.leafGroup.rotation.set(rx,ry,rz);if(stateType==='specialize'&&wither){u.leaf.material.color.setRGB(lerp(.41,.25,wither),lerp(.56,.17,wither),lerp(.26,.06,wither));u.leaf.material.emissive.setRGB(.06,.05*(1-wither),.02);u.leafGroup.scale.set(1,lerp(1,.82,wither),1)}if(par.hi>0){const a=par.hi; u.waterMat.shininess