=lerp(20,260,clamp(a)); u.surfaceMat.opacity=lerp(.76,.985,clamp(a*.92)); u.waterMat.emissive.lerp(new THREE.Color(0x1a3f58),clamp(a*.62)); u.surfaceMat.emissive.lerp(new THREE.Color(0x366f95),clamp(a*.55)); wet.intensity=3.4*clamp(a*1.05); if(a>.02){u.hiSurface.visible=true; u.hiSurface.material.opacity=clamp(.18+a*.52,.18,.96)} if(a>.07){u.hiBody.visible=true; u.hiBody.material.opacity=clamp((a-.01)*.90,.16,.78); u.waterMat.opacity=lerp(.67,.12,clamp((a-.04)*1.18)); u.surfaceMat.opacity=lerp(.76,.08,clamp((a-.10)*1.22))} u.hiSurface.scale.set(lerp(1,1.012,clamp(a*.9)),1,lerp(1,1.012,clamp(a*.9))); const pos=u.hiSurface.geometry.attributes.position, base=u.hiSurface.geometry.userData.base; for(let i=0;i<pos.count;i++){const x=base[i*3], z=base[i*3+2], rr=Math.hypot(x,z), wave=(Math.sin(x*8+s*2.5)+Math.cos(z*7-s*1.9)+Math.sin(rr*13-s*3.1))*.011*a*(a>1.3?2.75:a>1?2.1:1.35); pos.setXYZ(i,x,wave,z)} pos.needsUpdate=true; u.hiSurface.geometry.computeVertexNormals() }if(par.hue!==null && par.hm>0){const tw=new THREE.Color().setHSL(par.hue,.84,.47), ts=new THREE.Color().setHSL((par.hue+.028)%1,.92,.62); u.waterMat.color.copy(BASE_WATER).lerp(tw,par.hm); u.surfaceMat.color.copy(BASE_SURFACE).lerp(ts,par.hm)}if(par.rb>0){u.rainbow.visible=true; u.rainbow.material.opacity=.78*par.rb; u.rainbow.scale.y=newH/BASE.height; u.rainbow.position.y=u.water.position.y; const pos=u.rainbow.geometry.attributes.position,col=u.rainbow.geometry.attributes.color,c=new THREE.Color(); for(let i=0;i<pos.count;i++){const y=pos.getY(i), band=Math.floor((y*4.2+s*3.3+Math.atan2(pos.getZ(i),pos.getX(i))*.7)*2)/2, h=(emitHue+(((band%6)+6)%6)/6)%1; c.setHSL(h,.95,.56); col.setXYZ(i,c.r,c.g,c.b)} col.needsUpdate=true} else u.rainbow.visible=false;if(par.fl>0){const tick=Math.floor((s+seed*.0001)*4.2), h=(emitHue+(tick%6)/6)%1, c1=new THREE.Color().setHSL(h,.98,.50), c2=new THREE.Color().setHSL((h+.11)%1,.98,.64); u.waterMat.color.lerp(c1,par.fl); u.surfaceMat.color.lerp(c2,par.fl); u.waterMat.emissive.copy(c1).multiplyScalar(.18*par.fl); u.surfaceMat.emissive.copy(c2).multiplyScalar(.16*par.fl)}if(par.overflow>0||overflowExcess>0){const o=Math.max(par.ov||0,clamp(overflowExcess/.20)); u.spillLip.visible=true; u.spillLip.material.opacity=.60*o; u.spillLip.position.y=BASE.rimY-.006+Math.sin(s*2.1)*.0025*o; u.overflow.children.slice(1).forEach((d,i)=>{const gate=clamp((o-d.userData.phase)*2.2); d.visible=gate>.04; d.material.opacity=.38*gate; const wob=.008*Math.sin(s*1.6+i*1.1)*gate; d.position.set(Math.cos(d.userData.ang)*(1.09+wob),.19,Math.sin(d.userData.ang)*(1.09+wob)); d.rotation.set(0,d.userData.ang,Math.sin(s*.9+i)*.03*gate)}) } else {u.spillLip.visible=false; u.overflow.children.slice(1).forEach(d=>d.visible=false)}if(par.cry>0){for(const [i,m] of u.crystals.children.entries()){const grow=clamp((par.cry-m.userData.ph)*1.8); m.scale.setScalar(lerp(.01,m.userData.w,grow)); m.scale.y=lerp(.001,m.userData.h,grow); m.rotation.y=s*.9+i}} if(stateType==='specialize'&&boil){for(const [i,b] of u.bubbles.children.entries()){const ph=(s*b.userData.sp+b.userData.ph)%1, lift=(1-ph)*1.2*boil; b.position.set(b.userData.x,-.85+lift,b.userData.z); b.material.opacity=.08+.28*boil*(1-ph); b.scale.setScalar(.5+.7*boil)}} if(stateType==='specialize'&&black){u.waterMat.color.lerp(new THREE.Color(0x0b0b13),black*.85);u.surfaceMat.color.lerp(new THREE.Color(0x11111b),black*.9)} if(stateType==='specialize'&&pulse){const q=.95+Math.sin(s*3.8)*.06*pulse; u.leafGroup.scale.set(q,1.0,q); u.surface.scale.set(1+.02*pulse,1,1+.02*pulse)} if(stateType==='specialize'&&u.clones.children.length){u.clones.children.forEach((c,i)=>{c.visible=true; c.position.set(Math.sin(s*.8+i)*.12,.02*i,Math.cos(s*.9+i)*.12); c.rotation.set(.02*i,s*.4+i,-.04*i); c.scale.setScalar(.92); c.children.forEach(ch=>{if(ch.material){ch.material.transparent=true; ch.material.opacity=.28}})})}if(echo){u.echoes.children.forEach((e,i)=>{e.visible=true;e.position.y=u.surface.position.y+.07+i*.085+Math.sin(s*1.2+i)*.012;e.scale.setScalar(1+i*.055);e.material.opacity=(.18-i*.04)*echo})}else u.echoes.children.forEach(e=>e.visible=false);if(pulse){const sc=1+Math.sin(s*3.8)*.055*pulse;u.water.scale.x=u.water.scale.z=sc;u.surface.scale.set(sc,sc,1);u.hiBody.scale.x=u.hiBody.scale.z=sc;u.hiSurface.scale.x=u.hiSurface.scale.z=sc}if(reverse){const up=.18*reverse*(.5+.5*Math.sin(s*1.4));u.surface.position.y+=up;u.hiSurface.position.y+=up;u.leafGroup.position.y+=up*.9}if(glitch){const q=Math.floor(s*10);root.rotation.z=noise(q+501,seed)*.03*glitch;root.position.x=noise(q+701,seed)*.035*glitch;root.position.y=-.52+noise(q+901,seed)*.018*glitch}else{root.rotation.z=0;root.position.x=0;root.position.y=-.52}if(timeskip&&Math.floor(s*5)%5===0)root.rotation.y+=noise(Math.floor(s*5)+811,seed)*.10*timeskip;renderer.render(scene,camera); if(p>=1&&!shown){shown=true; setTimeout(showResult,150)} }typeBtns.forEach(btn=>btn.onclick=()=>{typeBtns.forEach(x=>x.classList.toggle('on',x===btn)); stateType=btn.dataset.t; shown=false; prepareRun()}); lvBtns.forEach(btn=>btn.onclick=()=>{lvBtns.forEach(x=>x.classList.toggle('on',x===btn)); stateLevel=+btn.dataset.l; shown=false; prepareRun()}); document.querySelector('#replay').onclick=()=>{shown=false; prepareRun()}; document.querySelector('#reseed').onclick=()=>{seed=Math.floor(Math.random()*900000)+100000; shown=false; prepareRun()};prepareRun(); requestAnimationFrame(anim);